import 'package:flutter/material.dart';

void main() {
  runApp(BPMoniterIotApp());
}

class BPMoniterIotApp extends StatelessWidget {
  const BPMoniterIotApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
